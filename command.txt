#git命令

git config --list

git config --global user.name "BeiDai"

git config --global user.email " "

cat .gitconfig

git log 

#上传本地仓库

git init

git status

git add XXX/ 或 .

git commit -m "XXXX"

git commit -a -m "XXX"

git show 哈希码

git remote add origin https://github.com/BeiDai2/STM32F407ZGT6_MDK.git

git remote -v

git push -u origin master

#同步远程仓库

git pull origin master

#克隆远程仓库

git clone https:\\github.com\BeiDai2\XXX

# SSH 协议配置

ssh-Keygen -t rsa -C "emailadress"

ls ~/.ssh/

cat id_rsa.pub

复制到git服务端 SSH Keys



